HOW TO BUILD PWBINARYC.EXE

open cmd and type: "pogas.cmd [COMMAND]"

Commands:
	build (= Build the MAIN.py)
	updater (= Build the Updater.py)

.EXE's are in the dist/ folder! [will be created, after build]